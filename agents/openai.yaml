interface:
  display_name: "GitHub Project Publisher"
  short_description: "Publish and update GitHub projects"
  default_prompt: "Use $github-project-publisher to prepare or update local projects for GitHub, write bilingual READMEs, and push them safely."
policy:
  allow_implicit_invocation: true
