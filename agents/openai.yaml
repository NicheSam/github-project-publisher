interface:
  display_name: "GitHub Project Publisher"
  short_description: "Publish local projects to GitHub"
  default_prompt: "Use $github-project-publisher to prepare local projects for GitHub, write bilingual READMEs, and push them safely."
policy:
  allow_implicit_invocation: true
